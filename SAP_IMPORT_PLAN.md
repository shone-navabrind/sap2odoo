# What to import into SAP next

Generated by `python -m src.import_plan`. Every statement below is derived from files on
disk, not from memory: the live probe (`schema_snapshots/service_probe.json`), the parsed
service definitions (`schema_snapshots/custom_service_index.json`) and `src/registry.py`.

## How to import

In the ByDesign tenant: **Application and User Management -> OData Services -> Custom OData
Services -> Import**, then upload the `.xml` file named below from
`byd-api-samples-main/Custom OData Services/`. After importing, run
`python -m src.probe_services` to confirm it went live, then `python -m src.main`.

## Current state: 38 of 47 services are live

Already imported and returning data:

```
  khbusinesspartner
  khbusinesspartnerrelationship
  khbusinessresidence
  khcostcentre
  khcustomer
  khcustomerinvoice
  khcustomerinvoicerequest
  khcustomerquote
  khcustomerreturn
  khemployee
  khemployeetime
  khfunctionalunit
  khgoodsandactivityconfirmation
  khgoodsandserviceacknowledgement
  khhousebankaccount
  khhousebankstatement
  khinbounddelivery
  khlead
  khlocation
  khopportunity
  khoutbounddelivery
  khoutbounddeliveryrequest
  khpayment
  khproductionorder
  khprofitcentre
  khproject
  khpurchaseorder
  khsalesarrangement
  khsalesorder
  khserviceproduct
  khserviceproductvaluationdata
  khsupplier
  khsupplierinvoice
  tmserviceconfirmation
  tmserviceorder
  tmservicerequest
  vmumaterial
  vmumaterialvaluationdata
```

## Priority 3 - Optional - supporting detail, no sheet object depends on it alone

### `khcompany.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Company master with default currency - feeds res.company setup.
- **Exposes:** 11 entity sets, 47 fields total
- **Largest entity sets:** `AddressSnapshotPostalAddressCollection` (10 fields), `NameCollection` (5 fields), `AttributesCollection` (5 fields), `DefaultCurrencyCollection` (5 fields), `DefinitionCollection` (5 fields)

### `khconfirmedinbounddelivery.xml`

- **Would complete:** #32 Stock Transfers
- **Why:** Confirmed inbound deliveries; complements khinbounddelivery.
- **Exposes:** 10 entity sets, 46 fields total
- **Largest entity sets:** `ConfirmedInboundDeliveryCollection` (11 fields), `ItemCollection` (7 fields), `DateCollection` (4 fields), `ArrivalPeriodCollection` (4 fields), `DeliveryQuantityCollection` (4 fields)

### `khaddresssnapshot.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Resolves the address snapshot IDs that documents reference, turning internal address keys into real postal addresses.
- **Exposes:** 5 entity sets, 45 fields total
- **Largest entity sets:** `AddressSnapshotCollection` (20 fields), `PostalAddressCollection` (12 fields), `NameCollection` (6 fields), `DisplayNameCollection` (4 fields), `FormattedAddressCollection` (3 fields)

### `khproductionplanningorder.xml`

- **Would complete:** #45 Production History
- **Why:** Planned production orders, ahead of released manufacturing orders.
- **Exposes:** 3 entity sets, 39 fields total
- **Largest entity sets:** `MainMaterialOutputCollection` (19 fields), `ProductionPlanningOrderCollection` (17 fields), `ProductionRequisitionCollection` (3 fields)

### `khreportinglineunit.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Reporting line hierarchy; enriches the employee org assignment.
- **Exposes:** 6 entity sets, 31 fields total
- **Largest entity sets:** `CurrentResponsibleManagerCollection` (12 fields), `CurrentNameCollection` (5 fields), `BusinessCharacterCollection` (5 fields), `ReportingLineUnitCollection` (3 fields), `CurrentSuperordinateReportingLineUnitCollection` (3 fields)

### `khcompanypaymentfileregister.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Incoming payment file register; enriches bank statement handling.
- **Exposes:** 3 entity sets, 27 fields total
- **Largest entity sets:** `AttachmentFolderCollection` (19 fields), `IncomingFileCollection` (6 fields), `CompanyPaymentFileRegisterCollection` (2 fields)

### `khpaymentorderprocessingstatement.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Payment order processing statements (24 fields); enriches payments.
- **Exposes:** 2 entity sets, 26 fields total
- **Largest entity sets:** `PaymentOrderProcessingStatementCollection` (24 fields), `CompanyNameCollection` (2 fields)

### `khinbounddeliveryrequest.xml`

- **Would complete:** #32 Stock Transfers
- **Why:** Requested inbound deliveries.
- **Exposes:** 4 entity sets, 20 fields total
- **Largest entity sets:** `ItemCollection` (7 fields), `ConfirmationItemQuantityCollection` (7 fields), `InboundDeliveryRequestCollection` (4 fields), `ConfirmationItemCollection` (2 fields)

### `khtimeagreement.xml`

- **Would complete:** no single sheet object - supporting data
- **Why:** Employee time agreements. Not on the 66-object sheet.
- **Exposes:** 3 entity sets, 20 fields total
- **Largest entity sets:** `ItemCollection` (12 fields), `EmployeeTimeAgreementCollection` (6 fields), `EmployeeTypeCollection` (2 fields)

## Not solvable by importing a service

These migration-sheet objects have **no matching service among the 47 `.xml` files at all**.
Searching every parsed entity-set name across all 47 services for `glaccount`, `chartof`,
`ledger`, `billofmaterial`, `bom`, `productionmodel`, `exchangerate`, `asset`, `depreciation`
and `journal` returns zero matches. Importing more services will not produce them.

| Sheet # | Object | Mandatory | What it would need instead |
|---|---|---|---|
| 1 | Chart of Accounts | Yes | A ByDesign Financials OData service, or a manual export from *General Ledger -> Charts of Accounts* in the tenant UI |
| 2 | Taxes | Yes | Partially derivable from tax codes actually used on invoices (see `account_tax.csv`); full rates need *Tax Management* configuration export |
| 40 | BOMs | Yes | ByDesign's Production Model / Bill of Material; no OData service definition exists in this sample set |
| 14, 15 | Fixed Assets, Depreciation | No | ByDesign Fixed Asset module export |
| 13 | Journal Entries | No | Financials journal OData service |
| 0 | Currency Exchange Rates | No | ByDesign currency conversion configuration |

## Blocked by authorisation, not by import

These services **are already imported and live**, but every data read returns
`RBAM_ERROR: Not Authorized: Check Authorization Restriction for the User`. The fix is an SAP
role change for the technical user, not another import. Re-tested against the live tenant
on 2026-09-24 - `khcustomerquote` and `khcustomerreturn` were removed from this list, see
below:

| Service | Sheet object | Work center the integration user needs |
|---|---|---|
| `khproject` | #7 Analytic Accounts | **Project Management** |
| `khlead` | #19 Activities | **Leads** |
| `tmserviceorder` | (service orders) | **Service Orders** |
| `tmservicerequest` | (service requests) | **Service Requests** |

**No longer blocked (confirmed 2026-09-24 against the live tenant):** `khcustomerquote` and
`tmserviceconfirmation` are live, authorised, and return HTTP 200 - just 0 rows, meaning
there is genuinely no quote/service-confirmation data in this tenant, not an access problem.
`khcustomerreturn` is live, authorised, **and has 810 real rows** in `CustomerReturnCollection`
- this is now extracted (was never wired into `SOURCES` at all until the 2026-09-24
coverage_gap backfill) but not yet mapped to an Odoo object; #66 Credit Notes is closest but
not verified as the same concept - worth a dedicated look.

## Previously thought broken on SAP's side - now confirmed working

`khgoodsandactivityconfirmation`'s `InventoryChangeItemCollection` and
`GoodsAndActivityConfirmationCollection` used to return **HTTP 500 Internal Server Error**
on the original test tenant even for a two-row request. Re-tested against the live tenant on
2026-09-24: both now return full data cleanly - 366,585 and 147,979 rows respectively. This
was apparently a defect specific to the old tenant (or since fixed by SAP), not the query or
this codebase. No action needed; kept here as a record in case it recurs on a future tenant.
